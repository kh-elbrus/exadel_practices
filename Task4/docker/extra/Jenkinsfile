pipeline {
    agent { 
        dockerfile {
            filename './Task4/docker/extra/Dockerfile'
            args '-u root:sudo -t web-service'
        } 
    }
    environment {
        PASSWORD = credentials('JENKINS-SECRET-TEXT')
    }
    stages {
        stage('Run container') {
            steps {
                sh 'docker run -d -p 0.0.0.0:5000:5000 -t web-service'
            }
        }
        stage('Check running containers') {
            steps {
                sh 'docker ps -a'
            }
        }
    }
}