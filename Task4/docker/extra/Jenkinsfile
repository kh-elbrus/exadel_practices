pipeline {
    agent { 
        dockerfile {
            filename './Task4/docker/extra/Dockerfile'
            args '-u root:sudo'
        } 
    }
    environment {
        PASSWORD = credentials('JENKINS-SECRET-TEXT')
    }
    stages {
        stage('Run container') {
            steps {
                sh 'docker run -it -d -p 5000:5000'
            }
        }
        stage('Check running containers') {
            steps {
                sh 'docker ps -a'
            }
        }
    }
}